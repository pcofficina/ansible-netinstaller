[global]
        bind interfaces only = yes
        interfaces = {{ lan_ip }}

        workgroup = PCO
        security = user
        passdb backend = tdbsam

        map to guest = Bad User
        log file = /var/log/samba/%m
        log level = 1

        server role = standalone server
        acl allow execute always = True

[win10]
        path = {{ os_basedir }}/win11/
        browseable = yes
        read only = yes
        guest only = yes
        guest ok = yes
