server {
       listen {{ lan_ip }}:80 default_server;
       # listen [::]:80 default_server;

       server_name _;

       root {{os_basedir}};
       # index index.html;

       location / {
		autoindex on;
		try_files $uri $uri/ =404;
       }
}
